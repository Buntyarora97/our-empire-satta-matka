services:
  - type: web
    name: our-empire-satta-matka
    runtime: node
    plan: free
    buildCommand: npm install pnpm --no-save && npx pnpm install --no-frozen-lockfile && npx pnpm approve-builds --yes && BASE_PATH=/ PORT=3000 npx pnpm --filter @workspace/admin-panel run build && npx pnpm --filter @workspace/api-server run build
    startCommand: node --enable-source-maps ./artifacts/api-server/dist/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: LOG_LEVEL
        value: info
      - key: GIT_LFS_SKIP_SMUDGE
        value: "1"
